I'm going to unit test this code and would like you to structure the response so that it's unit testable. I don't want you to modify the essence or logic of the code, just make the absolute minimal set of changes such that my test harness could run this code to evaluate its correctness against my expectations. 

Do not write any new code or functionality. You should only make modifications to encapsulate the functionality provided.

The unit tests I have assume the following:

<insert function signatures w/ input and output contracts>  --------- EXPLAIN ???

long long sumOfPrimes(int n) {
   
}






















2. Tasks are not enough and concern about the throughput.

3. Can we fallback to Colosseum campaign if I cannot find doable tasks.