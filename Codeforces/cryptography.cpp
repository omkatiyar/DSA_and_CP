#include <bits/stdc++.h>
using namespace std;

int main(){
    int t; cin>>t;
    while(t--){
        int n; cin>>n;
        // map<int ,int> mp;
        cout<<2<<" "<<n-1<<endl;
        // vector<int>v; 
        // vector<int>u;
        // for(int i=2;i<=n;i++){
        //     mp[n%i]++;
        //     // v.push_back(i);
        //     if(mp[n%i]>=2){
        //         int m= n- (n%i);
        //         vector<int> factors;
        //         for (int i = 2; i * i <= m; ++i) {
        //             if (m % i == 0) {
        //                 factors.push_back(i); // Add the smaller factor
        //                 if (i != m / i) {
        //                     factors.push_back(m / i); // Add the larger factor
        //                 }
        //             }
        //         }
        //         cout<<i<<" "<<(factors[0] == i? factors[1]:factors[0])<<endl;
        //         break;
        //     }
        // }
    }
}